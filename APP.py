import streamlit as st
import numpy as np
import pandas as pd
from math import sqrt
from datetime import datetime
import hashlib
import io
import base64

# El resto del código de la app debe funcionar solo con estas librerías.
# Elimina cualquier referencia a plotly, reportlab, fpdf, weasyprint, matplotlib y sus variables asociadas.
# Mantén solo tablas, texto y lógica de cálculo usando streamlit, numpy y pandas.

# (Aquí va el resto de tu código, ya limpio de dependencias externas)